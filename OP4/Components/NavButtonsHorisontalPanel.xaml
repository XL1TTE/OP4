<UserControl x:Class="OP4.Components.NavButtonsHorisontalPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OP4.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
            <RadioButton Style="{StaticResource NavButtonStyle}"
                     Content="Генерация текста"
                         IsChecked="True"
                         Command="{Binding SwitchToTextGenerationMenu}"/>
            <RadioButton Style="{StaticResource NavButtonStyle}"
                     Content="Информация о тексте"
                         Margin="5 0 0 0"
                         Command="{Binding SwitchToTextInfoMenu}"/>
        </StackPanel>
    </Grid>
</UserControl>
